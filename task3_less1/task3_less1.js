//  Реализовать функцию drawCalendar , которая принимает три аргумента - год, месяц,
// htmlElement и выводит в этот элемент календарь на месяц (дни недели начинаются с понедельника ).
drawCalendar = function (year,month,element) {
    calendar().of(year, 0);
}

(()=> {
    // actions
    const setCalendar = document.querySelector('#calendar');

    // result
    setAction.addEventListener(, ()=> {
        drawCalendar(2019,1,#calendar));
        render();
    });
})()
